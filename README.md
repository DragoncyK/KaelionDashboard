# Frontend deployment

This folder is ready for GitHub Pages.

## Edit first

Open `config.js` and update:

- `API_BASE_URL`
- `BOT_INVITE_URL`

## GitHub Pages

Push this folder to your repository and enable GitHub Pages from the repo settings.

The frontend expects the backend to be running on your VPS.
